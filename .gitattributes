# 自动处理换行符，将所有文本文件的换行符统一为 LF 存储在 Git 中，
# 并在检出时根据操作系统自动转换（Windows 转换为 CRLF，Linux/Mac 保持 LF）
* text=auto

# 强制某些类型的文件在检出时保持 LF（例如 shell 脚本等在 Windows 环境下也需要 LF 的文件）
*.sh text eol=lf
*.bash text eol=lf

# 二进制文件不进行换行符转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
