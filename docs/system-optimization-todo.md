# 系统优化 TODO（产品经理视角）

生成日期：2026-03-23  
范围：前端功能、UI 展示、后端 Agent 系统

## 现状快照

- 前端构建失败：`npm run build` 报错模块不存在（`frontend/src/App.tsx` 引用缺失）。
- 后端单测存在回归：流式聊天相关测试失败（SSE 协议与实现不一致）。
- 接口安全边界不足：部分会话/管理/上传/WebSocket 接口缺少严格鉴权与归属校验。

---

## P0（本周必须完成）

- [x] 前端技术栈收敛为 Vue 单栈，移除 React 残留入口、依赖与配置，恢复可构建状态。
- [x] 统一 SSE 协议：后端首包补齐 `meta + conversation_id`，与前端流式解析逻辑对齐。
- [x] 修复 `ChatService` 在缓存命中分支中的潜在未定义变量问题（同步模式）。
- [x] 会话接口补齐归属校验：会话详情、消息、更新、删除都必须校验“当前用户拥有该会话”。
- [x] 管理后台核心监控接口增加鉴权与管理员角色校验。
- [x] 上传接口与 WebSocket 接口增加鉴权与限流防护。
- [x] 配置安全加固：禁止默认 `SECRET_KEY`，修复 `DEBUG` 环境变量解析脆弱点。

## P1（下个迭代）

- [ ] Chat 页面拆分为组件（消息流、侧栏、输入区、附件区、动作按钮区），降低单文件复杂度。
- [ ] 修复 Chat 页面模板冗余/重复片段，清理结构性 UI 噪声。
- [ ] 将“分享/置顶/反馈”等占位能力变成真实可用能力（不再仅提示“开发中”）。
- [ ] 会话列表批量删除改为后端批处理 API，避免前端逐条串行删除。
- [ ] 消息历史增加分页/懒加载，优化长会话性能与可用性。
- [ ] 统一通知连接池实现，打通“转人工通知 -> 管理端实时接收”链路。
- [ ] 管理端指标可信化：补真实模型维度、链路字段，去除 `print`，统一结构化日志。
- [ ] 中间件策略修正：放行真实健康检查路径，收敛 CORS 白名单策略。

## P2（体验与长期）

- [ ] 统一视觉语言（中英文文案、配色、组件规范、交互风格）。
- [ ] 增加“停止生成/重试/断线恢复”能力，完善流式交互体验。
- [ ] 建立 CI 质量门禁：前端构建、后端单测、核心 API 合约测试。
- [ ] 为 Agent 工作流增加端到端回放测试，减少“代码与测试预期漂移”。

---

## 验收标准（Definition of Done）

- [ ] `frontend` 可稳定通过 `npm run build`。
- [ ] 后端 `tests/unit` 通过率恢复至稳定基线（关键流式测试全部通过）。
- [ ] 关键接口完成鉴权与权限覆盖，并补充对应测试。
- [ ] 管理端核心监控页面数据来源可追溯，错误处理统一。
- [ ] 发布前完成一次完整回归（登录、会话、流式聊天、附件、管理后台）。

## 建议排期（两周）

- 第 1 周：P0 全量完成 + 核心回归。
- 第 2 周：P1 重点项（页面拆分、批量删除、监控可信化）+ 预埋 P2 基础设施（CI）。
